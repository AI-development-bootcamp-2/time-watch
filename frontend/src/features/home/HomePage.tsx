import LogoutButton from '../../components/LogoutButton'

// Simple home placeholder; renders the logout control alongside the page label
export default function HomePage() {
  return <div>Home <LogoutButton /></div>
}
